apiVersion: v1
kind: LimitRange
metadata:
  name: tenant-a-limits
  namespace: deployaz-demo
spec:
  limits:
    - type: Container
      default:
        cpu: 250m
        memory: 256Mi
      defaultRequest:
        cpu: 100m
        memory: 128Mi
